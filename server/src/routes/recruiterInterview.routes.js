import { Router } from "express"; import authenticate from "../middlewares/auth.middleware.js"; import authorize from "../middlewares/authorize.middleware.js"; import validateRequest from "../middlewares/validateRequest.middleware.js"; import { USER_ROLES } from "../constants/app.constants.js"; import * as c from "../controllers/interview.controller.js"; import * as v from "../validators/interview.validator.js";
const r=Router();r.post("/applications/:applicationId",authenticate,authorize(USER_ROLES.RECRUITER),v.scheduleValidator,validateRequest,c.schedule);r.get("/",authenticate,authorize(USER_ROLES.RECRUITER),v.listValidator,validateRequest,c.recruiterList);r.get("/:interviewId",authenticate,authorize(USER_ROLES.RECRUITER),v.interviewIdValidator,validateRequest,c.recruiterDetail);r.get("/:interviewId/history",authenticate,authorize(USER_ROLES.RECRUITER),v.interviewIdValidator,validateRequest,c.history);r.patch("/:interviewId/reschedule",authenticate,authorize(USER_ROLES.RECRUITER),v.rescheduleValidator,validateRequest,c.reschedule);r.patch("/:interviewId/cancel",authenticate,authorize(USER_ROLES.RECRUITER),v.reasonValidator,validateRequest,c.cancel);r.patch("/:interviewId/complete",authenticate,authorize(USER_ROLES.RECRUITER),v.interviewIdValidator,validateRequest,c.complete);r.put("/:interviewId/feedback",authenticate,authorize(USER_ROLES.RECRUITER),v.feedbackValidator,validateRequest,c.feedback);export default r;
