import { param,query } from "express-validator"; export const savedJobIdValidator=[param("jobId").isUUID(4).withMessage("A valid job ID is required.")]; export const savedJobListValidator=[query("page").optional().isInt({min:1}),query("limit").optional().isInt({min:1,max:100}),query("search").optional().isString().trim().isLength({max:200}),query("sort").optional().isIn(["createdAt","updatedAt","title"]),query("order").optional().isIn(["ASC","DESC","asc","desc"])];
