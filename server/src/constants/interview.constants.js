export const INTERVIEW_STATUSES = Object.freeze({
  SCHEDULED: "SCHEDULED", RESCHEDULED: "RESCHEDULED", CONFIRMED: "CONFIRMED",
  DECLINED: "DECLINED", CANCELLED: "CANCELLED", COMPLETED: "COMPLETED"
});
export const INTERVIEW_STATUS_VALUES = Object.freeze(Object.values(INTERVIEW_STATUSES));
export const MEETING_TYPES = Object.freeze({ ONLINE: "ONLINE", IN_PERSON: "IN_PERSON", PHONE: "PHONE" });
export const MEETING_TYPE_VALUES = Object.freeze(Object.values(MEETING_TYPES));
export const ACTIVE_INTERVIEW_STATUSES = Object.freeze([
  INTERVIEW_STATUSES.SCHEDULED, INTERVIEW_STATUSES.RESCHEDULED, INTERVIEW_STATUSES.CONFIRMED
]);
export const INTERVIEW_TRANSITIONS = Object.freeze({
  SCHEDULED: ["CONFIRMED", "DECLINED", "RESCHEDULED", "CANCELLED"],
  RESCHEDULED: ["CONFIRMED", "DECLINED", "RESCHEDULED", "CANCELLED"],
  CONFIRMED: ["RESCHEDULED", "CANCELLED", "COMPLETED"],
  DECLINED: ["RESCHEDULED", "CANCELLED"], CANCELLED: [], COMPLETED: []
});
export const INTERVIEW_LIMITS = Object.freeze({
  LOCATION: 500, PHONE_INSTRUCTIONS: 500, INSTRUCTIONS: 5000, REASON: 1000,
  FEEDBACK: 5000, STRENGTHS: 3000, CONCERNS: 3000, RECOMMENDATION: 1000
});
